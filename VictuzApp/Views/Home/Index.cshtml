@model VictuzApp.ViewModels.HomePageViewModel

@{
    ViewData["Title"] = "Home Page";
}

<h2>Activiteiten van de maand:</h2>
<ul>
    @foreach (var bestactivity in Model.BestActivities)
    {
        <li><strong>@bestactivity.Name</strong>: @bestactivity.Description</li>
    }
</ul>

<h2>Aankomende 3 evenementen:</h2>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Title
            </th>
            <th>
                Description
            </th>
            <th>
                Date
            </th>
            <th>
                MaxParticipants
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Events)
        {
            <tr class="event-row">
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MaxParticipants)
                </td>


                    <td>
                    <!-- Leeg laten -->
                    </td>
            </tr>
        }


    </tbody>

</table>
<div class="home-button">
    <button class="button" onclick="window.location.href='https://localhost:7057/Events'">Inschrijven</button>
</div>


